# 基于背景折射畸变的透明液体液位视觉识别 —— 研究计划与合成数据方案

> 状态：Phase 0（最小物理拟真验证）进行中 · 建立日期 2026-09-02 · 渲染器 pbrt-v4（CPU 构建，`../pbrt-v4/build/pbrt.exe`）
> 本文档是后续开发与研究的总纲。每个阶段结束后在 §9 追加实验记录，不要覆盖历史结论。

---

## 1. 一句话目标

用**本地物理渲染器**大规模合成"透明容器 + 液体 + 结构化发光背景"的图像与精确液位真值，
训练能在真实相机画面中**快速、鲁棒**判读液位的 AI 模型；核心手段是利用**液体与空气折射率不同**
导致背景图案在液面上下产生**可测的几何畸变差异**，而不是依赖液体本身的颜色或液面反光。

## 2. 核心假设（可证伪）

| 编号 | 假设 | 验证方式 | 状态 |
|---|---|---|---|
| H1 | 纯白均匀背光下，透明液体（水）的液位仅靠视觉很难快速、准确辨别；可见线索只剩弯月面/菲涅尔亮暗带，且弱、窄、易被遮挡 | 渲染白板背景不同液位，计算液位上下区域的图像差异（应接近 0）；相邻液位（Δh=4 mm）差分图能量 | **Phase 0 已验证并修正**：液体段对白背景的改变 ΔE≈4（图案 65–79）；液位线只由弯月面产生（去掉弯月面后 4 mm 变化的差分 1.1 < 噪声底 1.3，线对比度 0.30→0.005） |
| H2 | 在容器后方放置结构化图案（棋盘格 / 马赛克 / 网格），液体段与空气段对背景的折射畸变不同（充液圆柱 = 柱面透镜，横向放大并**左右翻转**；空段 ≈ 薄壁玻璃管，畸变弱），液位线在图像上表现为**畸变模式的突变**，肉眼与算法都可快速定位 | 同上，对比图案背景 vs 白板的差分能量与逐行差分曲线峰值位置是否与真值行一致 | **Phase 0 成立**：液体段周期比空气段放大 3.5×（60% 孔径中位）并翻转，与独立光线追迹一致到 1%；差分过渡行与真值行一致（偏差 = 弯月面高度） |
| H3 | 物理渲染的合成数据（域随机化：容器形态、液体折射率与颜色、背景图案、壁面瑕疵、遮挡）足以训练出可迁移到真实相机的液位估计模型（sim2real gap 可接受） | Phase 3 训练 + Phase 4 实物光箱验证 | 待做 |
| H4 | 图案设计可以优化：存在使"最小可分辨液位差 Δh_min"最小、且对容器/液体参数变化最不敏感的背景图案族（例如彩色编码马赛克优于周期棋盘格，因为周期图案在特定放大率下会发生混叠） | 以 Δh_min 与混叠率为指标做图案消融 | 待做 |

## 3. 物理原理与定量预期

### 3.1 折射率
空气 1.000，水 1.333（20 °C, 589 nm），乙醇 1.361，甘油 1.473，硅油 ~1.40，玻璃 1.47（硼硅）–1.52（钠钙），PMMA 1.49，PET 1.575。
色散：水在可见光内 1.331–1.344，3 mm 玻璃壁与 15 cm 水路径的色散在相机分辩率下可忽略；pbrt 支持 `"spectrum eta" "glass-BK7"` 光谱 IOR，Phase 2 作为可选随机化项。

### 3.2 充液圆柱 = 柱面厚透镜

**定性结论**（已被 Phase 0 渲染与独立光线追迹证实）：
- 液体段把背景**横向放大并左右翻转**，竖向放大率 ≈ 1（柱面透镜不改变竖向）→ 棋盘格方格在液面以下变成横向拉长的矩形，随机马赛克可直接看出左右翻转；
- 空气段（空玻璃圆柱、壁厚 3 mm）两层薄壁近轴光焦度接近 0，背景仅被轻微**缩小**（MVP 几何下约 0.92×），基本原样透过；
- **液面上下畸变模式的差异就是液位线**，这是 H2 的物理基础。

**定量**：最简单的平行光薄透镜公式（f = nR/(2(n−1)) ≈ 15 cm，M_h ≈ f/(D−f) ≈ 1.5）**严重低估**放大率，原因是相机距离有限（60 cm）、孔径就是整个圆柱、且薄玻璃壁的光焦度不可忽略。
近轴厚透镜逐面追迹（含 0.3 cm 玻璃壁，n_g=1.5）给出：相机光线经 空气|玻璃(R=7.8)|水(R=7.5)|玻璃|空气 后，
近轴焦点位于轴后约 21.5 cm，板在 D=25 cm 处只比焦点远 3.5 cm，因此**图像中心的局部横向放大约 8.7×**；
离轴光线球差使放大率向边缘迅速下降，容器中央 60% 孔径上的**中位放大约 3.5×**（周期比），整体呈强烈的非均匀拉伸。
精确的二维几何光学追迹实现在 `scripts/mvp_compare.py::trace_panel_x`，Phase 0 渲染测得的周期比与之一致（误差 ~1%，见 `outputs/mvp/report.html` §7b）。

**设计含义**：
- **板到容器的距离 D 是最敏感的设计参数**：D 接近焦区（本几何约 20–23 cm）时放大率发散、图案在液体段"消失"成大色块；D 明显小于焦距时不翻转且温和放大；D 远大于焦距时翻转并缩小到 ≲1×。Phase 1 需要扫描 D，Phase 2 随机化时避开焦区或把它作为困难样本。
- 图案格子尺寸要与 D 联合选择：液体段可见的板宽度 ≈ 容器宽度 / 平均放大率，格子必须小于这个宽度的 1/3 才能在液体段留下 ≥3 个周期；同时格子在空气段不能小于相机分辩率。
- 周期图案在特定 (D, 格尺寸) 下会与空气段周期成整数比而产生混叠（§10.2），彩色编码马赛克没有此问题。

### 3.3 其他真实线索（合成时必须建模，否则 sim2real 失真）
- **弯月面**：水在洁净玻璃上接触角≈0°，毛细长度 l_c=2.7 mm，壁面处爬升高 h_m≈2.5–4 mm，轮廓 z(r) ≈ h_m·exp(−(R−r)/l_c)。弯月面在液位线附近形成一条暗/亮细带，是真实照片里白背景下**唯一**的液位线索。
- **菲涅尔反射与全反射**：相机高于液面时，液面对背景板发生掠射反射（亮带）；低于液面时透过液面看到板上方区域，出现全反射暗带。
- **吸收**：纯水 σ_a(R,G,B) ≈ (0.0035, 0.0015, 0.0003) /cm，15 cm 路径红光透过率 ≈ 0.95，肉眼几乎不可见；有色液体用 `homogeneous` 介质 `sigma_a` 建模，浊液加 `sigma_s`。
- **玻璃壁本身的折射位移**：真值"液位线像素行"应取内壁处液面近边缘的几何投影；穿过 3 mm 玻璃的视线偏移 < 0.1 mm，可忽略但要在真值定义里写明。

## 4. Phase 0 最小实验环境（本次实现）

单位 cm，z 向上，容器轴为 z 轴，容器底面外表面在 z=0。

| 项 | 设定 | 说明 |
|---|---|---|
| 容器 | 圆柱，内径 15.0（R_in=7.5），内高 20.0，壁厚 0.3，底厚 0.5，玻璃 η=1.50，无颈无盖 | 对应用户描述"直径 15 cm 高 20 cm 左右" |
| 液体 | 水 η=1.333，`homogeneous` 介质弱吸收；液位 = 填充率 × 内高 | 填充率 {0, 0.25, 0.50, 0.75} + 灵敏度对 {0.50, 0.52}（Δh = 4 mm） |
| 液体/玻璃接触 | **无空气隙**：内壁在液位处切成两段，湿段 `dielectric eta = η_w/η_g`，干段 `eta = 1/η_g`；底内面 `eta = η_g/η_w`；自由液面 `eta = η_w`；水体用 `MediumInterface` 包围 | 修正旧管线 0.2 mm 假空气隙带来的错误全反射 |
| 弯月面 | 可开关（默认开），h_m=0.3 cm，l_c=0.27 cm，旋转面三角网格 | 见 §3.3 |
| 相机 | `perspective` 针孔，位于 (0, −60, 10)（侧面中点正前方、光轴水平，高度 = 容器半高），画幅 1080×1440 竖构图，横向 fov 24° → 竖向 31.6°，在容器前壁处覆盖 ±11 cm × ±14.7 cm | 顶、底均在画内且留边 |
| 发光板 | 自发光 `AreaLightSource "diffuse"`，平面 50×60 cm，中心 (0, +25, 10)，法线朝相机；图案用 `"string filename"` 贴图发光，均匀白用 `rgb L` | 在相机视野内上下左右均超出容器；Lambertian 发光 = 理想匀光板 |
| 图案 | `white` 均匀；`rg_checker` 红/绿棋盘格，格 2.0 cm；`mosaic` 随机彩色马赛克，格 1.5 cm（局部唯一颜色码，可显现左右翻转）；`grid` 白底黑网格线，1 cm 间距、1 mm 线宽，5 cm 粗线 | 贴图 50 px/cm |
| 支撑 | 哑光黑圆台（半径 4，高 6）托起容器，使板在容器下方仍可见 | 之后可换桌面/透明托架 |
| 环境 | 无其他光源（暗室光箱） | 前壁菲涅尔反射为黑，属最干净基线；Phase 2 加房间环境贴图 |
| 积分器 | `volpath` maxdepth 64，`zsobol` 采样，1024 spp，gaussian 像素滤波，EXR 线性输出 + sRGB PNG | 高质量首轮视觉验证 |

输出：`outputs/mvp/renders/<pattern>_f<fill>.{exr,png}`；对比图 `outputs/mvp/compare/`（拼图、与空容器的差分、0.50 vs 0.52 差分、逐行差分曲线）；`outputs/mvp/report.html`；数值 `outputs/mvp/metrics.json`。

## 5. 真值（Ground Truth）定义

1. **物理量**：液面高度 h（cm，自内底面起）与填充率 h/H；相机内外参；容器几何。
2. **图像量**：液位线像素行 `gt_row_near`（前内壁处液面边缘的投影行）、`gt_row_far`（后内壁处）；液体掩膜、液面掩膜、容器 ROI（沿用旧管线的自发光 GT 通道法：目标物设为发光、其他设为 `interface`、`maxdepth 0`）。
3. **解析校验**：用 pbrt 的 LookAt/透视/screen-window 约定解析投影液面边缘，与渲染 GT 互检（旧管线已验证差 <1 px）。
4. 数据集清单 `dataset.json` 逐样本记录全部随机化参数，保证可复现（seed）。

## 6. 域随机化（Phase 2 数据生成器设计）

| 维度 | 随机项 | 实现要点 |
|---|---|---|
| 容器形态 | 圆柱/方槽/锥形烧杯/带颈瓶/量筒；半径、高、壁厚、底厚、倒角；玻璃 vs PMMA vs PET（η、色散、轻微吸收/黄化） | 旋转面网格生成器（半径随高度的分段折线/样条），可覆盖大多数器皿 |
| 液体 | η ∈ [1.33, 1.50]；颜色 `sigma_a`（透明→深色）；浊度 `sigma_s`；多层液体（油/水分层）；泡沫与气泡（小球 dielectric） | 分层 = 多段介质 + 多个界面 |
| 液面 | 静止水平 / 倾斜（容器倾斜 ±5°）/ 微波纹（正弦/噪声高度场网格）/ 弯月面高度与接触角 | 波纹网格 + 法线；弯月面 h_m∈[0,0.5] cm |
| 背景板 | 白、棋盘格（格尺寸、颜色对）、彩色马赛克（格尺寸、随机种子、色域）、网格线、条纹、De Bruijn 编码、随机自然图片、文字；亮度、色温、亮度渐变不均匀、板距 D、板倾角、板尺寸不足以覆盖 | 贴图程序生成；D 需覆盖 §3.2 的放大率范围 |
| 相机 | 位置抖动（高度、方位 ±20°、俯仰 ±10°）、焦距、薄透镜景深、曝光/ISO、白平衡、传感器噪声、镜头畸变、运动模糊、JPEG 压缩 | pbrt `perspective` 的 `lensradius/focaldistance`；后处理噪声/畸变在 Python 完成 |
| 壁面瑕疵 | 水垢/水渍（`coateddiffuse`/`dielectric` 粗糙度贴图 + 薄层吸收贴图，分布偏向液位线附近历史水位）、指纹油膜、划痕、水滴挂壁（半球 dielectric）、雾化（`roughness`） | 用程序噪声 + 掩膜贴图作为 `texture roughness`/`texture reflectance` |
| 遮挡 | 标签贴纸、刻度线、夹具、前景物体、手；容器局部出画 | 遮挡也要生成对应掩膜，供损失加权 |
| 环境 | 房间 HDR 环境（前壁反射）、额外顶光/侧光、板外杂光 | `infinite` 环境贴图 |

每个样本记录：`fill, h, R, H, t, eta_liquid, eta_glass, pattern params, D, camera, defects, occluders, seed` → 用于按因子分析模型误差。

## 7. 评价指标与基线

- **几何**：液位线行误差（px）→ 换算 mm（按容器像素高度）；填充率误差；分箱分类准确率（例如 5% 一档）。
- **可辨性（Phase 0 关键指标）**：
  - `D_level(pattern, fill)` = 容器 ROI 内 |I(fill) − I(empty)| 的均值（液体段与空段的畸变差异强度）；
  - `D_sens(pattern)` = |I(0.52) − I(0.50)| 的均值（4 mm 液位变化引起的图像变化）；
  - 逐行差分曲线 `rowdiff(y)` 的峰值行与 `gt_row` 的偏差；
  - 白板 vs 图案背景的 `D_sens` 比值即为 H1/H2 的定量证据。
- **基线检测器**：行梯度/行颜色阶跃（旧管线）、图案周期估计（对棋盘格做逐行水平频率分析，频率突变行 = 液位）、光流/相位相关（与空容器参考图的位移场）。
- **学习模型**（Phase 3）：直接回归液位行（CNN/ViT），或分割液体掩膜后取上边缘；比较不同背景族下的误差与 sim2real 差距。

## 8. 路线图

| Phase | 内容 | 产出 | 状态 |
|---|---|---|---|
| 0 | 本文档 + MVP 环境（§4）+ 4 图案 × 5 液位高质量渲染 + 差分对比与指标 | `docs/RESEARCH.md`, `scripts/mvp_*.py`, `outputs/mvp/` | 进行中 |
| 1 | 把 MVP 场景接入 GT 通道（掩膜、液位行），液位细扫（0→100%，步长 2%），板距 D 与格尺寸扫描，验证 §3.2 公式 | 灵敏度曲线 Δh_min(pattern, D, cell) | 待做 |
| 2 | 域随机化数据生成器（§6），GPU 构建（安装 OptiX SDK 后 `build_pbrt.bat -DPBRT_OPTIX7_PATH=...`，RTX 5090 预计单张 <2 s），万级样本 | `dataset/` + `dataset.json` | 待做 |
| 3 | 训练/评测液位模型；按因子分析误差；图案消融（H4） | 模型 + 评测报告 | 待做 |
| 4 | 实物验证：15 cm 玻璃缸 + 平板光箱/显示器显示同款图案 + 相机；采集真值（尺/称重）；微调与 sim2real 评估 | 真实测试集 | 待做 |

## 9. 实验记录（按时间追加）

- **2026-09-02 Phase 0 启动**：整理假设与物理预期；发现旧管线两处保真问题 —— (a) 液体与玻璃间 0.2 mm 假空气隙，(b) 128 spp / 640×480 噪点大；新 MVP 采用无空气隙嵌套介质与 1024 spp。本机环境：32 核 CPU、RTX 5090（尚无 OptiX SDK，pbrt 为 CPU 构建）；Python 虚拟环境来自另一台机器不可用，用 uv 重建 3.11。
- **2026-09-02 Phase 0 完成**（`outputs/mvp/report.html`，`outputs/mvp/compare/full/metrics.json`）。
  渲染：1080×1440、512 spp、volpath maxdepth 64，4 图案 × 5 液位 + 2 换种子对照 + 4 平液面对照 + 总览，共 27 张，32 线程 85 分钟（约 200 s/张）。
  512 spp 的选择依据：128 spp 时最平坦区域相对噪声 1.3%，512 spp 约 0.6%（≈1 个 8 位灰度级）；两种子差分的噪声底 ΔE（液位带内）= 1.3–1.5。
  结果（容器中央 60% 列，1 px ≈ 0.21 mm）：
  | 指标 | white | R/G checker (2 cm) | mosaic (1.5 cm) | grid (1 cm) |
  |---|---|---|---|---|
  | 液体段 vs 空容器 ΔE（fill 0.50，液面以下） | 4.3 | 65.1 | 78.9 | 23.0 |
  | +4 mm（0.50→0.52）液位带内 ΔE | 27.8 | 62.1 | 66.2 | 32.2 |
  | 同上，平液面（无弯月面） | **1.1**（< 噪声底 1.3） | 34.3 | — | — |
  | 单图液位线对比度 fill 0.50（有 / 无弯月面） | 0.297 / 0.005 | 0.183 / 0.004 | — | — |
  | 差分过渡行 − 真值行（近壁，fill 0.50/0.75） | −17 / −15 px | −15 / −13 | −13 / −11 | −15 / −13 |
  棋盘格横向周期（px）：板直视 119.5（追迹 119.6），透过空容器 111.7（追迹 111.1，0.93×），透过液体 390.6，中平面行带 393.5（追迹 393.5）；液体/空气比 3.50（追迹 3.54），竖向比 1.03–1.05；图像中心局部放大 8.7×（追迹）。
  结论：(1) H2 成立且量级很大——图案背景下液体段的外观与空气段完全不同，4 mm 液位变化的信号是噪声底的 40 倍以上；(2) H1 需修正——白板下液位线**可见但完全依赖弯月面**（掠射菲涅尔/全反射带），去掉弯月面后信号消失；真实场景中弯月面受污染、接触角、晃动影响大，因此白板方案脆弱；(3) 差分过渡行系统性地落在几何液位上方 ≈ 弯月面高度（0.3 cm ≈ 14.5 px），真值口径必须明确"几何液面"还是"接触线"；(4) 空容器本身把背景缩小约 7%，也是一个可用的容器存在/几何线索；(5) 近轴平行光公式低估放大率 (2.1 vs 3.5)，已用精确二维追迹替代（§3.2），放大率对板距极敏感（板距 25 cm 只比近轴焦点远 3.5 cm）。
  下一步（Phase 1）：液位细扫（步长 2%）、板距 D 与格尺寸扫描、相机高度扫描（当前相机与 50% 液面几乎等高，是白板的最有利情形）、接入 GT 掩膜通道、无弯月面/不同接触角的弯月面族。

- **2026-09-02 Phase 1 测试集（`scripts/testset_*.py`，`outputs/testset/`）**：为网页差分工具生成"基准（空）/ 对比（有液）"图对。
  - 容器（旋转体，外剖面 r(z) 单值、|dr/dz| ≤ 1.4，内壁为法线偏置 t）：葫芦瓶（双高斯鼓包，1300 ml）、1 L 锥形瓶、广口试剂瓶（余弦肩）、圆底烧瓶（解析球面 + 颈 + 软木环）、以及 MVP 圆柱。
  - 壁面沉积（**可选，默认关闭**；首版 light/medium/heavy 三档被判定为过重、不符合实际，正式测试集为洁净玻璃，`--dirt` 可重新启用）：外壁 = `mix(带粗糙度贴图的玻璃, diffusetransmission 沉积)`，mix 的 amount 即覆盖率贴图（水线环、挂滴、斑块、雾状薄膜）；干内壁只带水线环。教训：pbrt 默认把 roughness 开平方映射为 alpha，0.02 的薄膜就变成明显毛玻璃，需 `remaproughness false` 且粗糙度只跟随较厚沉积。Phase 2 若要用脏污做域随机化，覆盖率应降到均值 1–5%、以稀疏水线环和少量斑点为主。
  - 相机：normal 24°/60 cm、wide 44°/34 cm、tele 11°/130 cm；板 60×80 cm。
  - 后处理：对比帧曝光 ±5%、亚像素平移 ±2.5 px，全部帧加散粒+读出噪声（0.8 亮度处 σ≈1.5%）。
  - 分层液体（圆柱、红绿棋盘格）：水/葵花油（Δn 0.14，油微黄）、水/正己烷（Δn 0.04，均无色，最难）、二氯甲烷/水（Δn 0.09，DCM 在下）、水/正己烷 白板对照。液-液界面为平盘（`eta = n_下/n_上`，n_下 < n_上 时翻转法线保持 eta ≥ 1），顶面带弯月面。
  - 全量渲染：900×1200、160 spp、volpath maxdepth 48 + regularize，18 张（8 基准 + 10 对比）共约 8 分钟（22–30 s/张）。
  - 观察（`outputs/testset/contact_sheet.png`）：异形容器的曲率把图案拉伸成随高度变化的条纹，液位处仍是清晰的畸变突变；heavy 沉积使背景局部不可见但"窗口"处仍可判读；
    液-液界面在棋盘格下表现为"周期跳变 + 一条细线"，油/水（Δn 0.14）明显，DCM/水（0.09）中等，正己烷/水（0.04）很弱但可见；白板下只剩细线。
    定量结论见下一条。
- **2026-09-02 Phase 1 评估：现有方法在异形容器与分层液体上的适用性**（`scripts/testset_eval.py` → `outputs/testset/eval/`；ROI = 容器内腔包围框，误差以前内壁真值行为准，1 px ≈ 0.25 mm）
  | 案例 | 真值行 | 单台阶（原始） | 单台阶（自适应） | 多液位 v2 |
  |---|---|---|---|---|
  | A1 葫芦瓶·棋盘 | 629 | −9 px | −12 px | 顶面 ✓；出现 2 个形状引起的假界面 |
  | A2 葫芦瓶·广角·马赛克 | 501 | 0 px | −4 px | 顶面 ✓；2 个假界面 |
  | A3 锥形瓶·长焦·网格 | 672 | −17 px | −15 px | 顶面 ✓，无假界面 |
  | A4 圆底烧瓶·棋盘 | 562 | −14 px | −12 px | 顶面 ✓；2 个假界面 |
  | A5 试剂瓶·广角·棋盘 | 589 | −14 px | −14 px | 顶面 ✓，无假界面 |
  | A6 锥形瓶·抬高相机·马赛克·茶 | 721 | −21 px | −21 px | 顶面 ✓，无假界面 |
  | B1 水/葵花油 (Δn 0.14) | 671 / 469 | 顶面 −8 | 顶面 −9 | 469→460，671→667 ✓✓ |
  | B2 水/正己烷 (Δn 0.04) | 671 / 469 | 顶面 −4 | 顶面 −5 | 469→464，671→665 ✓✓ |
  | B3 二氯甲烷/水 (Δn 0.09) | 751 / 509 | 顶面 −10 | 顶面 −10 | 509→499，751→739 ✓✓ |
  | B4 水/正己烷·白板 | 671 / 469 | +477（失败） | +496（失败） | 无放大率特征，不可用 |
  结论：(1) **单液体 + 图案背景**：现有"ROI 内逐行差分 + 最大台阶"方法在全部六种容器/镜头组合上成立，误差 0–21 px（≤ 2.8 % ROI 高度），系统性偏上，来源是弯月面顶端与俯视时液面远侧边缘，与 Phase 0 一致；自适应管线在有噪声/抖动/曝光漂移的图上与原始差分同精度，白板背景仍不可用。
  (2) **分层液体**：单台阶方法只能给出顶面，液-液界面需要新特征。每行的**折射放大率 M(r)**（把对比图行与按比例缩放的基准图行做归一化互相关搜索得到）在两种液体间有明确台阶（Δn 0.04 亦可见，log M 差约 0.15），配合"水平边缘结构断裂"确认（|∂x| 行签名在界面处突变、在棋盘格行边界处不变）后，三组分层液体的界面均被找到，误差 4–12 px。
  (3) **局限**：在葫芦瓶、圆底烧瓶等强曲率容器中，容器半径随高度变化本身就改变放大率并使边缘结构漂移，产生 1–2 个假界面候选；锥形/直壁容器无此问题。白板背景没有可用的结构信息。
  (4) **改进路线（SOTA 方向）**：a) 几何感知：用基准图的容器轮廓恢复剖面 r(z)，按 §3.2 的二维追迹预测单液体下的 M(r) 期望曲线，只把偏离期望的台阶判为界面（可消除形状假阳性）；b) 学习法：用本管线的合成数据训练逐行/逐像素模型（U-Net 或 SegFormer 类分割网络输出液体区域与界面行；或以 RAFT 等光流网络求"基准→对比"的折射位移场作为输入特征，位移场的幅值/方向在每种液体中近似分段恒定），这是 Phase 3 的主线；c) 纯视觉零样本模型（SAM 2 等）对透明液体分界面不可靠，只宜作交互式标注辅助。
  网页工具已实现 (2) 作为可选 Layers 模式，并内嵌 10 组案例与真值（顶栏按钮一键载入，显示 GT 与误差）。

- **2026-09-06 水垢样本基准图修正**：S1–S5、V1–V10 的基准图原本也带沉积，与"先拍空容器、之后才积垢"的使用场景不符；改为按 容器 × 相机 × 图案 共享的**洁净空容器基准**（`base_<vessel>_<camera>_<pattern>_dz<..>`），只有对比帧携带沉积。`scale_run.py` 重渲 27 张（20–45 s/张），`outputs/scale/cases.json` 记录每个案例的 baseline 名称；网页包重新打包（25 案例 / 45 图，675 px 宽 = 0.75 尺度）。
- **2026-09-06 液位检测 v3：几何优先、光度其次**（`scripts/level_detect.py`，网页 JS 同步移植；评估 `scripts/eval_all.py --scales=1,0.85,0.75,0.65,0.55 outputs/testset/cases.json outputs/scale/cases.json` → `outputs/eval_all/{eval_all.md,eval_all.json,profiles_*.png}`）。
  原理：液体改变背景图案的**几何**（每一行的水平放大率 / 翻转），而沉积、脏污、曝光漂移与传感器噪声只改变**光度**。因此主信号是"基准行 ↔ 对比行"的逐行刚性水平缩放（对 ROI 中央 70 % 列做加权归一化互相关，49 个对数等距尺度 × 正/镜像），液位就是放大率发生**跳变**的行；这是一种方法覆盖单液体、多液体、异形容器与沉积干扰，而不是逐案例打补丁。
  关键设计（每一条都对应一次失败案例的物理分析，见 `eval_all` 各尺度结果）：
  1. 相关只在**发生变化的列**上计算（未变化列权重 0.02）。否则全反射暗带的行会借助容器外未变化的背景列"匹配"到恒等变换而被误认为空气区（A1/A2 在不同分辩率下 −6 ～ −10 pt 的根源）。变化列不足 5 % 的行按定义即恒等（M = 1）；不足 20 % 的行不可测（少量水滴列会被 98 个候选变换过拟合，V7 的 +7.8 pt 根源）。
  2. 行分类：**恒等**（M = 1 假设的相关 ≥ 0.55 且不劣于最佳变换 0.15；洁净玻璃、雾状沉积、薄膜都属此类）、**液体**（可信变换且 |log M| ≥ 0.3 或镜像）、其余为**未知**（暗带、致密沉积、水滴引起的折射抖动）——未知行只当作透明的间隙，绝不作为证据。
  3. 跳变统计跨越间隙：以某行两侧最近的 w（= 6 % ROI 高）个可靠行分别做加权线性拟合，J = 两条拟合在该行的差，z = |J| / 鲁棒残差尺度；同一跳变在 w 与 2w 两个窗口下必须一致（曲率随窗口增长、噪声毛刺随窗口缩小，两者都被剔除）。
  4. 自由液面 = **恒等平台的终点**（空气区之下图案再也不会与基准重合），液位锚定在其下方第一处**暗行**（相对空气区亮度 < 0.92）：接触线 / 弯月面在透射下从任何视角都是一条暗线；从下方仰视时全反射暗带也始于液位；从上方俯视时"恒等终点 → 暗线"之间是液面远侧边缘的折射视图（S2/S4/V7 之前 +3 ～ +5 pt 的系统偏差即来源于此）。无暗行时退回到带内光度台阶或平台终点；跳变检测器给出的行只在平台终点 2w 之内被采纳。
  5. 次级边界（液-液界面）只在两侧平台稳定（残差 ≤ 0.12、z ≥ 15、2w 窗口下 z 不缩小、可靠行占比 ≥ 0.5）、**带有接触线亮度异常**（界面 ±1.5 % ROI 高内相对局部中值的亮或暗偏离 ≥ 6 %：两种液体在壁面相遇必有弯月面把光线偏折，B1 暗线 0.25、B3 亮带 0.24、B2 弱亮线 0.08–0.10；而球形/锥形容器内同一液体的放大率区段切换——焦散——在光度上是平滑的，A4 的假界面均 ≤ 0.04）且不在 ROI 底部 20 %（光线穿过容器底）时报告。
  6. 基准无图案（白板）时退化为光度尖峰（弯月面 / 界面暗线），只报最强一条并标注低置信度。
  结果（工作分辩率 1.0；误差 = 估计 − 前壁真值，单位为 ROI 高度百分点 pt；"旧"= 网页原先的逐行 ΔE 最大台阶）：
  | 案例 | 模式 | 真值 % | 估计 % | 误差 pt（每个真值） | 假阳性 | 旧 pt |
  |---|---|---|---|---|---|---|
  | A1_gourd_normal_checker | warp/identity-end | 45.6 | 47.3 | +1.7 | 0 | +1.6 |
  | A2_gourd_wide_mosaic | warp | 63.7 | 64.1 | +0.4 | 0 | +0.6 |
  | A3_erlenmeyer_tele_grid | warp | 38.7 | 40.7 | +2.0 | 0 | +2.3 |
  | A4_florence_normal_checker | warp | 54.2 | 56.0 | +1.8 | 0 | +1.8 |
  | A5_reagent_wide_checker | warp | 51.9 | 53.1 | +1.2 | 0 | +2.1 |
  | A6_erlenmeyer_normal_mosaic_tea | warp | 43.9 | 46.5 | +2.5 | 0 | +3.1 |
  | B1_cyl_water_oil_checker | warp | 39.9 / 65.2 | 65.7 / 40.1 | +0.2 / +0.5 | 0 | +1.1 |
  | B2_cyl_water_hexane_checker | warp | 39.9 / 65.2 | 65.4 / 39.8 | -0.1 / +0.2 | 0 | +0.6 |
  | B3_cyl_dcm_water_checker | warp | 29.8 / 60.1 | 61.4 / 30.6 | +0.8 / +1.3 | 0 | +1.3 |
  | B4_cyl_water_hexane_white | photometric | 39.9 / 65.2 | 66.9 | miss / +1.7 | 0 | -62.2 |
  | S1_cyl_vase_line60_water45 | warp | 45.0 | 44.4 | -0.6 | 0 | +2.2 |
  | S2_cyl_tide_water30_mosaic | warp | 29.8 | 30.1 | +0.3 | 0 | +4.8 |
  | S3_gourd_vase_line60_water40 | warp | 40.5 | 36.7 | -3.8 | 1 | +2.7 |
  | S4_cyl_vase_macro_checker | warp | 27.2 | 28.8 | +1.7 | 0 | +4.9 |
  | S5_cyl_vase_macro_white | photometric | 27.2 | 49.3 | miss | 1 | +23.7 |
  | V1_cyl_line30_water55 | warp | 55.1 | 56.6 | +1.6 | 0 | +1.5 |
  | V2_cyl_line50thin_water35_grid | warp | 34.9 | 36.1 | +1.2 | 0 | +1.5 |
  | V3_erl_line75heavy_water40_mosaic | warp/identity-end | 41.8 | 43.7 | +1.9 | 0 | +1.9 |
  | V4_reagent_line92full_water60 | warp | 61.3 | 62.7 | +1.3 | 0 | +1.4 |
  | V5_cyl_band4060_water50 | warp | 50.0 | 51.4 | +1.4 | 0 | +9.5 |
  | V6_gourd_tilted_water45_mosaic | warp | 45.6 | 47.6 | +2.0 | 0 | -10.0 |
  | V7_cyl_splash_water30_wide | warp | 29.8 | 30.9 | +1.0 | 0 | +7.4 |
  | V8_cyl_sector_water50 | warp | 50.0 | 51.6 | +1.6 | 0 | +1.7 |
  | V9_erl_dripsonly_water45_tele | warp | 45.8 | 48.0 | +2.1 | 0 | -41.5 |
  | V10_cyl_line50thin_water50_white | photometric | 50.0 | 51.6 | +1.6 | 0 | -47.0 |
  **跨尺度稳定性**（同一算法、同一参数在 1.0 / 0.85 / 0.75 / 0.65 / 0.55 五个工作分辩率下）：145 个界面中 136 个在 6 pt 内找到，7 个假阳性（全部是 S2/S3/S5 的顶面双解，无假次级界面），误差中位数 1.24 pt，最大 5.7 pt；21/25 个案例顶面估计的跨尺度极差 ≤ 1.0 pt（B1–B3 两个界面全部 ≤ 1.3 pt）。旧方法在沉积案例 V5 / V7 / V9 与白板案例上误差 7 ～ 47 pt，v3 在这些案例上 ≤ 2.5 pt。
  仍存在的局限：(a) 两个案例在两种解之间跳动：S3（葫芦瓶体上的水线环 + 薄膜，相机略高于液位）−3.8 / +2.6 pt；S2（潮痕，相机高出液位约 3 cm）+0.3 / +5.7 pt——其接触线暗行只比空气区暗 12 %，正处于 0.92 阈值边缘，网页 0.75 尺度下为 +5.6 pt（落到液面远侧边缘）；(b) 圆底烧瓶（A4）内的放大率有多个区段（NCC 地形图显示球体把图案放大到中央列只剩不到一个格，该带内任何缩放都匹配，加上下半球的焦散翻转），2026-09-06 晚加入的接触线判据后不再报为次级边界，但这也意味着 Δn 更小、弯月面更弱的液-液界面（如 B2 在 0.65 尺度以下）会被同一判据放弃；(c) 白板 + 致密水线环（S5）无法区分环与液位（光度模式固有，B4 亦无法给出液-液界面）；(d) DCM/水界面的显著性在低分辩率下降到阈值附近（B3 @0.85 漏检一次，B2 @0.65/0.55 漏检）；(e) 相机抬高 + 茶色液体（A6）仍有 +2.5 pt（远侧液面暗区与接触线难分）。下一步：用基准图容器轮廓恢复 r(z) 并按 §3.2 追迹预测单液体的 M(r) 期望，可消除 (b)；接触线检测改为亚像素亮度拟合可再压缩 1 pt 级偏差。
  网页：ANALYSIS 标签显示模式（WARP / IDENTITY-END / PHOTOMETRIC），新增 MODE 与 Z · JUMP 指标；Layers 只是显示开关，检测始终在对齐后的半分辩率图对上运行；拖动 ROI 时用廉价的光度预览，松手后运行完整检测（案例上 40–180 ms）。`window.LDB` 暴露当前分析记录供调试。

- **2026-09-06 网页重设计**：按栅格重排为「顶栏（品牌 + 案例栏一行 26 个案例）→ 三个等高图像台（基准 / 对比 / 分析）→ 一条结果栏」。结果栏只保留一个主读数（LEVEL，琥珀色大号等宽数字）和三项次级读数（真值、Δ pt、液-液界面及其 Δ），其余诊断量收进 DETAILS；检测模式作为胶囊标签放在分析台标题旁，增益 / 不透明度 / 混合调节放在分析台标题右侧。字体统一为 IBM Plex Sans + IBM Plex Mono，冷调深色底（#0c0f13）承托彩色渲染图，琥珀色只用于液位。响应式：≥ 1180 px 三列并铺满视口，760–1179 px 两列（分析台横跨），< 760 px 单列；所有文字禁止换行，案例栏横向滚动。

- **2026-09-06 夹具部分遮挡（`scripts/inserts_run.py` → `outputs/inserts/`，网页 R 组）**：在 `testset_scenes.build_scene` 中加入 `inserts`（不锈钢搅拌轴 Ø8 mm + 叶片、玻璃温度计 Ø6 mm、PTFE 加液管 Ø8 mm、注液液柱与液滴），夹具按液体边界拆段以给出正确的介质界面；R1 搅拌轴（两帧均有）、R2 加液管（仅对比帧插入并浸入液面下）、R3 反应釜组合（轴 + 温度计 + 浸管）、R4 锥形瓶 + 偏心金属棒 + 网格、R5 搅拌轴 + 水/油两层。结果：R1/R2/R3 在五个尺度下误差 0.9–2.2 pt、无假界面（部分遮挡不改变几何，被当作"未变化列"或普通遮挡忽略；仅对比帧才有的加液管同样不影响）；R5 两个界面在 1.0/0.85/0.55 尺度均被找到（≤ 2.2 pt），0.75/0.65 漏检下层界面；R4 在原始分辩率上偏差 −6.4 pt（其余尺度 +1.0～+1.6）：锥形瓶底部半径 6.5 cm 对应焦距 ≈ 13 cm，25 cm 的板距恰好接近 2f，水平放大率接近 ±1，几何信号几乎消失（网格图案左右对称、翻转不可见），这是装置几何而非夹具造成的退化，建议板距避开 2f 或改用非对称图案（马赛克）。
- **2026-09-06 注液视频（`scripts/pour_video.py` → `outputs/pour/`，网页 F 组 + 视频模式）**：PTFE 加液管以恒定流量向 15 cm 圆柱注水，液位 4 s 内从 12 % 升到 55 %，20 fps × 81 帧，450×600、32 spp（每帧约 3 s，快速测试质量）；每帧含从管口到液面的液柱（半径 3 mm，带轻微脉动）、两颗沿液柱下落的卫星液滴和撞击点附近 3 颗飞溅液滴，液面为平面 + 弯月面，相机固定、只加传感器噪声。基准帧 = 空容器 + 加液管。逐帧评估（`scripts/eval_video.py` → `outputs/pour/level_vs_time.png`）：81 帧全部检出，81 帧在 6 pt 内、58 帧在 3 pt 内，误差中位数 +0.83 pt，最大 5.4 pt，无假次级界面；低液位阶段（前 1 s）相机高出液面约 7 cm，液面远侧边缘带宽，误差偶达 +4～+5 pt。这一序列还暴露了一个检测器缺陷并已修正：当跳变检测器没有候选时，接触线暗行的搜索窗曾深入液体区 3w 行而落在液体内部的暗纹上（单帧 −14 pt），现在无候选时搜索止于第一段液体行。
  网页视频模式：F 组按钮或把视频文件拖到对比图位置（基准仍是一张空容器静帧）。播放时 `<video>` 原速播放，`requestVideoFrameCallback` 送来的每一帧在检测器空闲时被分析（跟不上时跳帧）；拖动进度条或按 , / . 逐帧精确定位（按帧号 seek）；空格播放/暂停。结果栏新增液位-时间轨迹：琥珀色点为逐帧原始估计、实线为 5 帧时间中值、绿色虚线为真值。上传视频默认按 20 fps 计时。

- **2026-09-07 视频时序稳定 + 接触线判据修订**：注液序列早期（液位 12–20 %）逐帧结果在两种解之间跳动 ±4 pt，原因不是杯底折射，而是两条系统偏差不同的"液面假说"交替被采纳：恒等平台终点（= 俯视时液面远侧边缘，偏高 2–4 pt）与接触线暗行（偏低 1–2 pt，且旧的 0.92 绝对亮度阈值会锁在棋盘格行边界这类静止亮度特征上，出现"卡住—跳变"锯齿）。两项修正：(1) 接触线判据改为看恒等终点下方那条带的性质——比空气区**亮**（面板在液面远侧边缘上的镜像，仅当液位在图像地平线以下即俯视时成立）则液位是亮带回落到空气亮度的那一行；比空气区**暗**（弯月面阴影 / 全反射带）则液位是暗带首行；两者皆非则取恒等终点。仰视时的亮带是被照亮液体的全反射镜像，液位在带的起点，用图像地平线区分。静态案例回归：145+30 个界面中 163 个在 6 pt 内，假阳性 8 → 5，中位误差 1.32 pt。(2) 视频加入**多假说时序跟踪**（Python `eval_video.track` 与网页同源）：检测器每帧输出接触线暗行、跳变定位行、恒等终点三条假说（都换算成液位百分比），远侧边缘带厚度 b = 终点 − 暗行 在两者同时存在时按 EMA 学习，使"终点 − b"在没有暗行的帧里仍可用；常速预测后取最接近预测的假说并以 α = 0.6 融合，偏离 > 3 pt 视为离群、滑行，连续三次离群则以最近三帧原始值的中值重新锁定；连续两帧不动而预测在动的假说被判为静止亮度特征而忽略；帧号不连续（拖动进度条）则重置。结果（实验室 HDRI 下重渲的序列）：逐帧 中位 +1.06 pt、p90 1.9、最大 3.3；跟踪后 中位 +0.77 pt、p90 1.6、最大 2.3，81/81 帧在 3 pt 内。网页主读数与液位线显示跟踪值，轨迹图中点为逐帧原始值、实线为跟踪值。
- **2026-09-07 实验室环境光（HDRI）与 K 组**：`scripts/env_map.py` 下载 Poly Haven 的 CC0 室内 HDRI（选用 `childrens_hospital`，白墙、顶部日光灯板、窗，2k），用 `imgtool makeequiarea` 转成 pbrt-v4 的等面积方图，按立体角加权平均亮度 0.355（99.9 分位 72，即灯板）归一化到相对面板 0.8 的 0.28 平均环境辐亮度；`build_scene` 用 `tex['env']`（文件、尺度、绕 z 旋转）替代常数环境项。K 组两个新容器：K1 广口试剂瓶 + 瓶颈内不锈钢搅拌棒 + 潮痕水垢（棋盘）、K2 葫芦瓶 + 钢棒 + 玻璃温度计 + 60 % 水线环与薄膜（马赛克）；对比帧的环境光旋转 12°、减弱 15 % 以模拟环境变化。结果：K1 +1.0～+2.0 pt、K2 +1.3～+3.1 pt，无假界面；金属棒呈现房间高光与灯板反射，玻璃上有环境映像。R 组与注液序列亦在同一 HDRI 下重渲：R1–R3 误差 0.2～1.7 pt 不变，R4 的 2f 退化不变；**R5 的水/油界面在 HDRI 下丢失**——顶部灯板在圆柱前壁上的镜面反射叠加在放大最强的水层上（放大后的棋盘格几乎均匀，透射图案纹理弱），而这一反射在基准帧与对比帧中相同，把该层的逐行缩放估计拉向恒等（M≈1 的镜像），界面跳变随之消失；油层（放大率 1.68）仍可测。这是真实的环境光干扰机理，实测中应加遮光罩或偏振片、降低顶灯在容器方向的反射，或把面板亮度提高到远超室内反射。

- **2026-09-07 注液视频扩展（`scripts/pour_video.py` 改为多序列，`outputs/pour/<序列>/`）**：在圆柱注液序列 F1 之外新增两个不同容器的注液视频，均含水垢、穿过瓶颈的 PTFE 加液管和实验室 HDRI 环境光，对比帧的环境光相对基准帧旋转 12°、减弱 15 %（与 K 组一致）：**F2** 锥形瓶 + 马赛克面板 + 75 % 处重铁锈色水线环及其下方薄膜，加液管偏心 8 mm 穿过瓶颈；**F3** 广口试剂瓶 + 红绿棋盘 + 潮痕水垢（60 → 45 % 台阶、飞溅、流痕），加液管偏心 12 mm。两者液位 4 s 内从 15 % 升到 55 %，20 fps × 81 帧，450×600、32 spp。基准帧仍是干净的空容器 + 加液管；水垢在每一帧都按当前液位区分干/湿——被淹没的部分随液位上升逐帧变为折射率匹配的湿垢，是随时间变化的干扰，而不是静态贴图。三段视频以帧内编码（每帧关键帧）嵌入网页，CRF 26，F1 1.4 MB、F2 1.7 MB、F3 1.4 MB，整页 15.2 MB。
  结果：F1（圆柱，重编码）逐帧 中位 +1.06 pt、p90 1.9、最大 3.3；跟踪后 中位 +0.77 pt、p90 1.6、最大 2.3，81/81 帧在 3 pt 内；F2（锥形瓶）逐帧 中位 +2.48 pt、p90 4.9、最大 6.2；跟踪后 中位 +1.98 pt、p90 4.2、最大 6.5，60/81 帧在 3 pt 内；F3（试剂瓶）逐帧 中位 +1.11 pt、p90 2.7、最大 4.7；跟踪后 中位 +1.04 pt、p90 2.1、最大 3.1，80/81 帧在 3 pt 内。F3 与 F1 相当。F2 系统性偏高 +2～+5 pt，低液位阶段最明显：这是俯视**圆锥**时液面远侧边缘带的几何偏差——锥形瓶下部半径大，液面椭圆在图像中占 4～8 行，恒等平台终点与接触线暗行都落在远侧边缘附近（前壁接触线在锥壁上表现为一条亮线而不是暗线，暗带规则取的是带的首行），随液位接近图像地平线偏差收敛到 ±2 pt；此外锥壁倾斜使液体区违背逐行水平缩放模型（竖直方向也发生折射），液体行大多不可测，在 50～57 % 处 |log M| ≈ 0.17 低于液体阈值 0.30，该段只能依赖恒等终点与接触线，个别帧的接触线锁在水垢膜下缘的静止暗纹上（−4～−6 pt）。锥形瓶在此几何下的读数应按"远侧边缘"理解，或把相机降到液位高度附近。
- **2026-09-07 检测器：恒等平台按可测行判定 + 液面下方必须是透镜**：F2 序列在 40～46 % 液位区间连续 13 帧把 75 % 处的水垢环当成液面（+37 pt）。隔离实验（同一帧分别去掉环境光变化 / 去掉水垢 / 用常数环境光重渲）表明触发条件是**环境光变化**：旋转、减弱后的灯板反射在锥壁上移位，使约 20 行空气行的恒等相关度跌破阈值而成为 unknown，加上水垢环的 8 行，恒等平台判据（25 行窗口内 ≥ 60 % 恒等行）再也凑不齐，`top` 为空，回退到跳变候选并锁在环上。这与设计原则"unknown 行不作证据"不一致——该判据把 unknown 行计入了分母。两项修正：(1) 平台终点只看可测行：窗口内可测行（恒等 + 液体）中恒等占 ≥ 60 % 且恒等行不少于 w 行；下方 long 行内恒等行 < 15 % 的条件不变（锥体里液体行常不可测，故下方仍按全部行计）。(2) 液面候选必须是状态变化：位于恒等终点上方的跳变候选与其下方的恒等行矛盾，仅当 |Δlog M| ≥ 0.30 时保留；并且液面下方必须是透镜——|log M| 跨越候选行必须增大（≥ J_MIN/2），水垢膜带的微弱折射（M ≈ 0.84）回到干净玻璃（M = 1）的台阶被排除（它曾在一帧里给出 +10 pt）。回归（`eval_all`，5 个尺度）：160 个案例×尺度条目中 8 个结果改变且全部改善（A3、K1、R4、V9 误差降低 0.5～1.6 pt），无新增假阳性；scales [0.55, 0.65, 0.75, 0.85, 1.0]: 170/185 interfaces found within 6 pt, 8 false positives, median |err| 1.28 pt, max |err| 5.68 pt（修正前中位 1.36 pt）。视频：F2 逐帧中位 +4.2 → +2.5 pt、最大 38.7 → 6.2；F3 跟踪后 3 pt 内帧数 71 → 80；F1 不变。网页 JS 同步。

- **2026-09-07 F2 误差归因：几何 + 算法，不是渲染质量**（`scripts/diag_pour_geometry.py`、`scripts/diag_pour_quality.py`，结果在 `outputs/pour/F2/geometry_rims.json`、`outputs/pour/quality/F2_quality.json`）。(1) 渲染质量对照：取 5 帧分别以 450×600 / 256 spp（采样 8×）和 900×1200 / 128 spp（像素 4×、采样 4×）重渲。同分辨率加采样后误差与原序列完全一致（+5.2 / +3.9 / +4.2 / +2.0 / −4.9 对 +5.2 / +4.5 / +4.2 / +2.6 / −4.9），高分辨率渲染缩回 450×600 后误差也逐帧相同——噪声不是原因。(2) 几何：`build_scene` 同时投影液面前沿（真值）与后沿。相机高 9.25 cm（容器半高），液位到 fill 0.488 才与相机等高；在此之前俯视液面，锥形瓶下部宽，后沿在图像里高出前沿 5.7 pt（fill 0.15）并线性收敛到 0。59 帧俯视帧中恒等终点位于几何后沿上方中位 +1.10 pt、MAD 仅 0.18 pt——检测量是一条真实、稳定的边（后沿 + 远壁弯月面），只是不是前沿；原始读数相对后沿中位 +0.64 pt、相对前沿 +3.0 pt。圆柱案例里俯视时后沿到前沿之间是面板的镜像亮带，接触线规则取"亮带回落处"即得前沿；锥形瓶里这段是暗的（映出房间），前沿接触线反而是斜壁上的一条亮线（ρ ≈ 1.2～1.3），暗带规则取带的首行即后沿。(3) 算法：更干净的渲染反而暴露第二个问题——900×1200 全分辨率下第 8、36 帧恒等终点跳到 66.5 %（+47 / +32 pt），256 spp 下恒等终点也移到 64 %（靠 first/dip 才没出错）。原因是水垢膜带（约 40～64 %）有微弱的正立缩小折射 M ≈ 0.85～0.9：噪声大时最佳缩放与恒等的 NCC 差不足 0.15，这些行仍算恒等；噪声小后差值超过 0.15，行被判 unknown，恒等平台在膜上缘结束，其下直到液面全是 unknown。这是恒等判据的缺陷而非噪声：正立缩小像（0 < M < 1，不翻转）不可能来自凸容器里的液体（液体是会聚透镜，只给出放大的正立虚像或翻转的实像），应视为空气侧证据（水垢/环境光），平台不该在这里结束。结论：F2 低液位 +3～+6 pt 是俯视圆锥的后沿几何偏差（可用已知相机高度与容器轮廓解析修正，或改用"亮线接触线"规则），46～57 % 段的 ±2～5 pt 与全分辨率失效是恒等判据对微弱正立缩小折射的误判；两者都不需要更高的渲染质量来解决。

## 10. 风险与开放问题

1. **H1 只部分成立（Phase 0 已量化）**：白背景下液位线由弯月面的掠射菲涅尔/全反射暗带给出（线对比度 0.30），平液面时信号低于渲染噪声底。真实照片里该线索受脏壁、接触角变化、液面晃动与遮挡影响，是最脆弱的线索；数据集必须随机化弯月面高度与接触角，评估时分别报告"有/无弯月面"两种情形。
2. **周期图案混叠**：棋盘格在特定放大率 M_h 下，液体段与空气段的格子可能对齐得几乎一致（M_h·cell ≈ k·cell），使局部难以判读；彩色随机马赛克无此问题，但需要相机色彩保真。
3. **sim2real 差异**：真实 LED 光箱非理想 Lambertian、有导光板纹理与亮度不均；显示器有摩尔纹、刷新；相机自动曝光/白平衡；这些需在 Phase 2 加入并在 Phase 4 校准。
4. **相机高度与液位重合**：相机光轴与液面等高时液面呈边缘视，是奇异配置；数据集必须随机化相机高度。
5. **GPU 构建**：pbrt-v4 GPU 路径需要 OptiX SDK（手动安装），且 `volpath` 与 `wavefront` 积分器在介质处理上有细微差异，需回归对比。
6. **真值口径**：真值定义为几何液位（内壁处），而算法可能学到弯月面顶或亮带位置，评估时需固定口径并允许 ±h_m 的系统偏差分析。

## 12. 水垢（CaCO₃ 沉积）形貌参考与物理沉积模型

### 12.1 参考来源（2026-09-03 调研，仅作观察，未复制任何图像）
- Hometalk 用户实拍：铅晶玻璃杯内壁硬水痕 — 曾经的水位以下整体呈均匀乳白薄雾、仍可透视，水线处略密，上边界柔和渐变，水线以上完全透明。
  https://www.hometalk.com/diy/clean/house/q-how-can-i-get-this-hard-water-stain-off-my-lead-crystal-vase-30540642
- Shutterstock 检索 "hard water stains glass" / "limescale glass"：淋浴玻璃上的干涸水滴——大量 1–5 mm 的圆形/椭圆残留斑，边缘密实、中心稀薄（咖啡环效应），并有向下的珠状流痕。
  https://www.shutterstock.com/search/hard-water-stains-glass
- Alamy 水壶水垢（RC042D、TBHJTG）：沸腾釜内壁/底部的厚层片状灰白结壳——这是"加热沉淀"体制，不是本项目的透明容器体制。
  https://www.alamy.com/stock-photo/limescale-kettle.html
- Wikipedia "Limescale"：颜色从灰白到粉/红棕（含铁），在蒸发区与热表面形成。 https://en.wikipedia.org/wiki/Limescale
- Qazi et al., *Salt creeping as a self-amplifying crystallization process*, Sci. Adv. 5, eaax1853 (2019)：结晶在气-液-固接触线处成核，弯月面越过晶体形成前驱膜，晶体沿壁向上"爬升"并呈自放大分枝；低相对湿度、高蒸发率、小接触角时更显著。 https://www.science.org/doi/10.1126/sciadv.aax1853
- 咖啡环效应综述（Sci. Rep. 8:12183 等）：液滴蒸发时溶质随毛细流向接触线聚集，形成边缘环。

### 12.2 观察到的形貌要点
1. 水线环：位于长期水位处，环上缘向上呈指状、分枝状扩散（爬升）1–5 mm，下缘较锐；环绕容器厚度不均，且水线常略倾斜。
2. 水线以下：均匀薄膜（乳白、可透视），越靠近最终蒸干位置越厚；带竖向排水条纹与 0.1–0.5 mm 的微晶颗粒感。
3. 分级下降的水位留下多条较淡的潮痕环。
4. 水线以上：溅落液滴干涸成边缘密实的圆环，部分拉长；从杯口向下的珠状流痕。
5. 浸没在水中的水垢因孔隙被水填充（1.59/1.33 而非 1.59/1.00）散射大幅减弱、明显变透明；干燥后又变白。
6. 颜色灰白偏暖，厚处可带铁锈黄棕。

### 12.3 模型（`scripts/scale_model.py`）与渲染（`scripts/testset_scenes.py` scale 材质，`scripts/scale_run.py`）
- 在内壁展开面 (s, z)（0.1 mm/px）上叠加光学厚度 τ：薄膜 τ_film·(1+g·深度分数)·条纹·颗粒；水线环 τ_ring·exp(−dz/L_up·指状因子)（上）/ exp(dz/L_down)（下），L_up≈3–4 mm，L_down≈1.2 mm；潮痕高斯带；泊松分布的液滴咖啡环（对数正态半径 0.4–4.5 mm，边缘宽 0.15R）；珠状流痕（Rayleigh–Plateau 周期 2–4 mm）；水线倾斜 ±1–2 mm 与环向调制。
- 光学：薄散射层与光子相互作用概率 P = 1 − e^{−τ} 作为 pbrt `mix(玻璃, 沉积)` 的 amount；沉积本身再按"厚度分数" 1 − e^{−τ/1.5} 在**前向散射**（粗糙介质透射，alpha≈0.22，保留背景轮廓）与**完全漫射**（`diffusetransmission`，反射率 0.22→0.60、透射率 0.72→0.34 随厚度变化，总反照率≈0.94，含铁略暖）之间混合；浸没段 τ/4.5 且前向 lobe 更窄。
- 教训：pbrt 的 `regularize` 会把任何非镜面 BSDF 在后续弹射中粗糙化到 alpha≥0.1，与水垢层同用会使整个容器变成毛玻璃，必须关闭。
- 样本：`outputs/scale/`（S1 圆柱 长期水线 60% + 现水位 45%；S2 阶梯潮痕 60→45% + 现水位 30%，马赛克背景；S3 葫芦瓶同历史；S4/S5 水线微距，棋盘格 / 白板），每个含 baseline（空、干沉积）与 compare（注水、浸没段变透明）。900×1200、192 spp，27–45 s/张。
- 位置/形态/面积变体 V1–V10（`scale_model.PRESETS`）：水线 30%（现水位在其上，环被浸没）、50% 细线无薄膜、75% 重铁锈色环（锥形瓶）、92% 近满整壁薄膜（试剂瓶）、40–60% 密集潮痕带、倾斜存放（葫芦瓶，水线倾斜 ±18 mm）、飞溅/倾倒为主（广角）、单侧 1/3 周向沉积、只有杯口流痕（长焦）、细线与现水位重合的白板最难例。
- 渲染结果（2026-09-03）：与参考照片一致的三个特征均再现——水线以下乳白薄膜且背景可透视、水线处密实环带并向上呈指状扩散、上方干涸水滴环；注水后浸没段透明度明显提高。注意在纯背光下沉积带呈中灰而非亮白（漫射透过的是板光的平均色），前光充足的真实照片里则是白色——环境光强度是 sim2real 匹配时需要标定的参数。这 5 组已并入网页案例栏（S1–S5）。

## 11. 目录与命令（Phase 0）

```
liquid_level_sim/
├─ docs/RESEARCH.md            本文档
├─ scripts/
│  ├─ mvp_patterns.py          生成发光板图案贴图 (outputs/mvp/patterns/*.png)
│  ├─ mvp_scene.py             15×20 cm 圆柱 + 水 + 板 + 相机 的 pbrt 场景生成（嵌套介质、弯月面）
│  ├─ mvp_render.py            批量渲染 + EXR→PNG
│  ├─ mvp_compare.py           差分图、逐行曲线、指标、report.html
│  └─ mvp_run.py               一键：patterns → scenes → render → compare
└─ outputs/mvp/                渲染、对比、报告
```

```bat
cd liquid_level_sim
.venv\Scripts\python scripts\mvp_run.py --smoke            REM 低分辩率快速检查几何
.venv\Scripts\python scripts\mvp_run.py --spp 1024         REM 高质量 4 图案 × 5 液位
```
